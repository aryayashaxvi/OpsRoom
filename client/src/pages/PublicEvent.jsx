import{useEffect,useState}from'react';import{useParams}from'react-router-dom';import api from'../api/client';export default function PublicEvent(){const{id}=useParams(),[data,setData]=useState(null);useEffect(()=>{api.get(`/events/${id}`).then(r=>setData(r.data)).catch(()=>{})},[id]);return <main className="public"><div className="brand">OPS<span>ROOM</span></div><h1>{data?.name||'Event information'}</h1><p>{data?.description||'This event is available to authorized participants.'}</p></main>}
