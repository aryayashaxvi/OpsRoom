import{useState}from'react';import{useNavigate}from'react-router-dom';import toast from'react-hot-toast';import{useAuth}from'../context/AuthContext';export default function Login(){const{login}=useAuth(),nav=useNavigate(),[register,setRegister]=useState(false),[form,setForm]=useState({name:'',email:'organizer@opsroom.dev',password:'Password123!'});const submit=async e=>{e.preventDefault();try{await login(form,register?'/auth/register':'/auth/login');nav('/')}catch(x){toast.error(x.message||'Unable to sign in')}};return <main className="auth"><section><div className="brand">OPS<span>ROOM</span></div><h1>Run the room.<br/>See everything.</h1><p>A live operations command center for the moments that matter.</p></section><form onSubmit={submit} className="login-card"><h2>{register?'Create your account':'Welcome back'}</h2>{register&&<input required placeholder="Full name" onChange={e=>setForm({...form,name:e.target.value})}/>}<input required type="email" placeholder="Email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><input required type="password" placeholder="Password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/><button>{register?'Create account':'Enter OpsRoom'}</button><button type="button" className="link" onClick={()=>setRegister(!register)}>{register?'Already registered? Sign in':'New here? Create an account'}</button></form></main>}
