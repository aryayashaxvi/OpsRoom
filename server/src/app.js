import express from 'express'; import cors from 'cors'; import helmet from 'helmet'; import rateLimit from 'express-rate-limit'; import morgan from 'morgan'; import routes from './routes/index.js'; const app=express();app.use(helmet());app.use(cors({origin:process.env.CLIENT_URL||'http://localhost:5173'}));app.use(express.json({limit:'1mb'}));app.use(morgan('dev'));app.use('/api/auth',rateLimit({windowMs:15*60e3,max:100}));app.use('/api/ai',rateLimit({windowMs:60e3,max:30}));app.use('/api',routes);app.use((err,req,res,next)=>{console.error(err.message);res.status(err.name==='ZodError'?400:500).json({success:false,message:err.name==='ZodError'?'Invalid request data':'Something went wrong'});});export default app;
